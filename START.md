# 🚀 Как запустить проект JobConnect

## Способ 1: Docker Compose (Рекомендуется) ⭐

Самый простой способ - запустить все одной командой через Docker.

### Шаг 1: Убедитесь, что установлен Docker

Проверьте установку Docker:
```bash
docker --version
docker-compose --version
```

Если не установлен, скачайте с [docker.com](https://www.docker.com/)

### Шаг 2: Запустите проект

Откройте терминал в корневой папке проекта и выполните:

```bash
docker-compose up --build
```

Эта команда:
- Соберет Docker образы для client и server
- Запустит MongoDB
- Запустит Backend API
- Запустит Frontend
- Запустит Mongo Express (опционально)

**Время первого запуска:** 3-5 минут (зависит от скорости интернета для загрузки образов)

### Шаг 3: Заполните базу данных тестовыми данными

В **новом терминале** (пока первый работает) выполните:

```bash
docker-compose exec api npm run seed
```

Это создаст:
- 2 работодателя (employer1@example.com, employer2@example.com)
- 2 соискателя (candidate1@example.com, candidate2@example.com)
- 2 компании
- 3 вакансии
- 3 заявки

### Шаг 4: Откройте в браузере

После запуска откройте:

- **Frontend:** http://localhost:3000
- **GraphQL Playground:** http://localhost:4000/graphql
- **Mongo Express (UI для БД):** http://localhost:8081
  - Логин: `admin`
  - Пароль: `admin`

### Тестовые учетные данные

После выполнения seed:

**Работодатель:**
- Email: `employer1@example.com`
- Password: `employer123`

**Соискатель:**
- Email: `candidate1@example.com`
- Password: `candidate123`

---

## Способ 2: Локальный запуск (без Docker)

Если хотите запустить без Docker для разработки.

### Требования

- Node.js 20+
- MongoDB (локально или MongoDB Atlas)

### Backend

1. Перейдите в папку server:
```bash
cd server
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
# Скопируйте .env.example в .env и заполните значения
NODE_ENV=development
PORT=4000
MONGO_URI=mongodb://localhost:27017/jobconnect
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d
WS_PORT=4001
```

4. Запустите сервер:
```bash
npm run dev
```

Backend будет доступен на http://localhost:4000

### Frontend

1. Откройте **новый терминал** и перейдите в папку client:
```bash
cd client
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env.local` на основе `.env.example`:
```bash
NEXT_PUBLIC_GRAPHQL_URL=http://localhost:4000/graphql
NEXT_PUBLIC_WS_URL=ws://localhost:4000/graphql
```

4. Запустите фронтенд:
```bash
npm run dev
```

Frontend будет доступен на http://localhost:3000

### Заполнение БД (локально)

В папке `server` выполните:
```bash
npm run seed
```

---

## Полезные команды

### Docker Compose

```bash
# Запуск в фоновом режиме
docker-compose up -d

# Остановка всех сервисов
docker-compose down

# Остановка с удалением данных БД
docker-compose down -v

# Просмотр логов
docker-compose logs -f

# Логи только API
docker-compose logs -f api

# Логи только клиента
docker-compose logs -f client

# Перезапуск сервиса
docker-compose restart api

# Выполнение команды в контейнере
docker-compose exec api npm test
```

### Локальная разработка

```bash
# Backend
cd server
npm run dev      # Запуск в режиме разработки
npm run build    # Сборка
npm start        # Запуск production версии
npm test         # Запуск тестов
npm run seed     # Заполнение БД

# Frontend
cd client
npm run dev      # Запуск в режиме разработки
npm run build    # Сборка
npm start        # Запуск production версии
npm run lint     # Проверка кода
```

---

## Troubleshooting (Решение проблем)

### Проблема: Порт уже занят

**Ошибка:** `Error: listen EADDRINUSE: address already in use :::4000`

**Решение:**
1. Найдите процесс, использующий порт:
```bash
# Windows
netstat -ano | findstr :4000

# Linux/Mac
lsof -i :4000
```

2. Остановите процесс или измените порт в `.env` файле

### Проблема: MongoDB не подключается

**Ошибка:** `MongoServerError: connect ECONNREFUSED`

**Решение:**
- Убедитесь, что MongoDB запущен
- Проверьте `MONGO_URI` в `.env`
- Для Docker: убедитесь, что контейнер `mongo` запущен

### Проблема: Docker образы не собираются

**Решение:**
```bash
# Очистите кэш и пересоберите
docker-compose build --no-cache
docker-compose up
```

### Проблема: Frontend не видит Backend

**Решение:**
- Проверьте, что Backend запущен на порту 4000
- Проверьте `NEXT_PUBLIC_GRAPHQL_URL` в `.env.local`
- Убедитесь, что нет CORS ошибок в консоли браузера

### Проблема: Тесты не проходят

**Решение:**
```bash
# Убедитесь, что MongoDB доступен для тестов
cd server
npm test
```

Если используется in-memory MongoDB, убедитесь, что `mongodb-memory-server` установлен.

---

## Проверка работоспособности

После запуска проверьте:

1. ✅ Backend отвечает: http://localhost:4000/health
2. ✅ GraphQL работает: http://localhost:4000/graphql
3. ✅ Frontend открывается: http://localhost:3000
4. ✅ Можно зарегистрироваться или войти с тестовыми данными
5. ✅ Можно создать вакансию (как работодатель)
6. ✅ Можно подать заявку (как соискатель)

---

## Быстрый старт (TL;DR)

```bash
# 1. Запустите все
docker-compose up --build

# 2. В новом терминале заполните БД
docker-compose exec api npm run seed

# 3. Откройте http://localhost:3000
```

Готово! 🎉

